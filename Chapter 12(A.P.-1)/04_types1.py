n : int = 5

name: str = "Harry"

def sum(a: int, b: int) -> int:  #(we created a function with two variables
       return a+b                #a nd b which takes the input as an i
                                 #integer and returns the output as a integer)
 
sum(3)    #if we write this now then python will show us 
          #the function that is being used to create sum of two
          #numbers(write "sum(3)" again to see this) 