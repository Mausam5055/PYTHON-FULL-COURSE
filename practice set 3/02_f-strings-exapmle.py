# More Complex Example:
# You can also perform calculations or call functions 
# within f-strings.

length = 5
width = 3
# Using f-string to calculate area and format the string
area_message = f"The area of a rectangle with length {length} and width {width} is {length * width} square units."

print(area_message)
