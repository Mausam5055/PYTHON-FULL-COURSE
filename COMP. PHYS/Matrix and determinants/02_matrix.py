import numpy as np  #This imports the NumPy library, which is commonly 
                    #abbreviated as np for easier access to its functions.
A = np . array ([[1 , 2 , 3] ,
[4 , 5 , 6]])  #np.array creates a NumPy array from a list 
                #(or lists) of values.
print (" Matrix A:\n", A )

#In Python, the \n is an escape sequence that represents a 
# newline character. It is used to insert a line break so that
#the text  appears in the next line
