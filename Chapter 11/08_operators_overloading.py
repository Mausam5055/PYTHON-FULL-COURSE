class Number:
    def __init__(self, n):
        self.n = n

n = Number(1)
m = Number(2)

print(n + m)


#if we run the above code we will get an error since python doent know
#what is meant by "+" symbol so we hav to 1st say python that 
# "+ " means to add to numbers 

#SEE NEXT FILE......